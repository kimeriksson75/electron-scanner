<!DOCTYPE html>
<html lang=en>
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self'">
    <title>Connect tag success</title>
    <Link rel="stylesheet" href="./styles/styles.css" />
    <Link rel="stylesheet" href="./styles/await-scan.css" />
    <Link rel="stylesheet" href="./styles/animated-icon.css" />
  </head>
  <body>
    <div class="welcome-wrapper">

      <div class="welcome-message">
        <h1>Hej där!</h1>
        <p>Scanna din RFID-tagg</p>
        <div class="lds-ripple"><div></div><div></div></div>
      </div>
        
        <div class="welcome-bookings">
          <h3>Bokningar <%= date %></h3>
          <ul>
            <% timeslots.map(item=> { %>
              <li class="calendar-day-bookings-animate calendar-day-bookings--occupied" key="<%= item._id%>">
                <div class="calendar-day-bookings__content__timeslot"><strong><%= item.timeslot %></strong></div>
                <div class="calendar-day-bookings__content__user"><%= item.username %></div>
              </li>
              <% }) %>
            </ul>
          </div>
        </div>
      </div>
  </body>
</html>